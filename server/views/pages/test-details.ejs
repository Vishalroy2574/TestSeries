<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>

<body>
    <%- include('../partials/header', { user }) %>

        <main class="container" style="padding-top: 4rem; padding-bottom: 4rem;">
            <div class="max-w-4xl mx-auto space-y-8">
                <!-- Header Component -->
                <div class="flex flex-col md:flex-row justify-between items-start gap-6">
                    <div class="flex-1">
                        <div class="flex items-center gap-3 mb-4">
                            <a href="/category/<%= test.category %>"
                                class="pill-outline text-[10px] uppercase font-bold tracking-widest">
                                <%= test.category %>
                            </a>
                            <% if (test.type && test.type.toUpperCase()==='PAID' ) { %>
                                <span class="pill-danger"
                                    style="background: var(--primary); font-size: 0.65rem; padding: 0.2rem 0.6rem;">PREMIUM</span>
                                <% } else { %>
                                    <span class="pill-outline"
                                        style="font-size: 0.65rem; padding: 0.2rem 0.6rem;">FREE</span>
                                    <% } %>
                        </div>
                        <h1 class="text-4xl font-extrabold text-slate-900 leading-tight">
                            <%= test.title %>
                        </h1>
                        <p class="mt-4 text-slate-500 text-lg max-w-2xl">
                            <%= test.description
                                || 'Master this subject with our expertly curated practice test series.' %>
                        </p>
                    </div>

                    <% if (test.type && test.type.toUpperCase()==='PAID' ) { %>
                        <div class="card glass p-6 text-center min-w-[180px]">
                            <div class="text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-1">Price</div>
                            <div class="text-3xl font-extrabold text-primary mb-4">₹<%= test.price || 0 %>
                            </div>
                            <% if (!hasPurchased) { %>
                                <a href="/tests/<%= test._id %>/purchase" class="btn-primary w-full"
                                    style="display:block;text-align:center;text-decoration:none;">Unlock Now</a>
                                <% } else { %>
                                    <div class="text-success font-bold text-sm">✓ Purchased</div>
                                    <% } %>
                        </div>
                        <% } %>
                </div>

                <div class="grid md:grid-cols-3 gap-8">
                    <!-- Sidebar Info -->
                    <div class="md:col-span-1 space-y-6">
                        <div class="card p-6">
                            <h3 class="text-sm font-bold text-slate-900 uppercase tracking-widest mb-4">Test Details
                            </h3>
                            <div class="space-y-4">
                                <div class="flex items-center justify-between py-2 border-b border-slate-50">
                                    <span class="text-xs text-slate-500">Duration</span>
                                    <span class="text-sm font-bold">
                                        <%= test.duration %> Minutes
                                    </span>
                                </div>
                                <div class="flex items-center justify-between py-2 border-b border-slate-50">
                                    <span class="text-xs text-slate-500">Questions</span>
                                    <span class="text-sm font-bold">
                                        <%= (test.questions && test.questions.length) || 0 %> Qs
                                    </span>
                                </div>
                                <div class="flex items-center justify-between py-2">
                                    <span class="text-xs text-slate-500">Access</span>
                                    <span class="text-sm font-bold">
                                        <%= (test.type || 'FREE' ).toUpperCase() %>
                                    </span>
                                </div>
                            </div>
                        </div>

                        <div class="p-6 bg-slate-900 rounded-2xl text-white">
                            <h3 class="text-sm font-bold uppercase tracking-widest mb-2">Need Help?</h3>
                            <p class="text-slate-400 text-xs mb-4">Contact our support team for any queries regarding
                                this test series.</p>
                            <a href="#" class="text-xs font-bold text-primary hover:text-white transition">Support
                                Center →</a>
                        </div>
                    </div>

                    <!-- Main Content / PDF -->
                    <div class="md:col-span-2">
                        <% if (test.type && test.type.toUpperCase()==='PAID' && !hasPurchased) { %>
                            <div
                                class="card glass p-10 text-center flex flex-col items-center justify-center min-h-[400px]">
                                <div
                                    class="h-20 w-20 bg-rose-50 rounded-full flex items-center justify-center text-3xl mb-6">
                                    🔒</div>
                                <h3 class="text-2xl font-bold text-slate-900 mb-2">Premium Content</h3>
                                <p class="text-slate-500 mb-8 max-w-sm">
                                    This test series is part of our premium catalog. Purchase it once to get lifetime
                                    access to the PDF and practice materials.
                                </p>
                                <a href="/tests/<%= test._id %>/purchase" class="btn-primary px-10"
                                    style="text-decoration:none;display:inline-block;text-align:center;">
                                    Unlock Now for ₹<%= test.price %>
                                </a>
                                <p class="mt-6 text-[10px] text-slate-400 uppercase tracking-widest">Secure Payment
                                    Processing</p>
                            </div>
                            <% } else { %>
                                <% if (test.pdfUrl && test.pdfUrl !=='undefined' && test.pdfUrl !=='null' ) { %>
                                    <div class="space-y-4">
                                        <div class="flex justify-between items-center">
                                            <h3 class="text-lg font-bold text-slate-900">Test Document</h3>
                                            <a href="/pdf/view/<%= test._id %>" target="_blank"
                                                class="text-xs font-bold text-primary hover:underline">Full Screen →</a>
                                        </div>
                                        <div class="card p-0 overflow-hidden shadow-2xl"
                                            style="border: 2px solid var(--primary); border-radius: 1.5rem;">
                                            <iframe src="/pdf/view/<%= test._id %>"
                                                style="width: 100%; height: 600px; border: none;"></iframe>
                                        </div>
                                    </div>
                                    <% } else { %>
                                        <div class="card glass p-10 text-center">
                                            <p class="text-slate-500">PDF not available for this test.</p>
                                        </div>
                                        <% } %>
                                            <% } %>
                    </div>
                </div>
            </div>
        </main>

        <%- include('../partials/footer') %>


</body>

</html>