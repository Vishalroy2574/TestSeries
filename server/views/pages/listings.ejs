<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        .listing-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 2rem;
            padding: 2rem 0;
        }

        .listing-card {
            border-radius: 1rem;
            overflow: hidden;
            transition: transform 0.2s;
            cursor: pointer;
        }

        .listing-card:hover {
            transform: translateY(-4px);
        }

        .listing-image {
            width: 100%;
            aspect-ratio: 1/1;
            object-fit: cover;
            border-radius: 1rem;
            background-color: #f3f4f6;
        }

        .listing-info {
            padding: 0.75rem 0;
        }

        .listing-title {
            font-weight: 700;
            color: #1a1a1a;
            font-size: 1rem;
            margin-bottom: 0.25rem;
        }

        .listing-subtitle {
            color: #717171;
            font-size: 0.875rem;
        }

        .listing-price {
            margin-top: 0.5rem;
            font-weight: 600;
            color: #1a1a1a;
        }

        .listing-price span {
            font-weight: 400;
        }
    </style>
</head>

<body>
    <%- include('../partials/header', { user }) %>

    <main class="container">
        <div class="listing-grid">
            <% tests.forEach(test => { %>
                <div class="listing-card" onclick="location.href='/tests/<%= test._id %>'">
                    <img src="https://images.unsplash.com/photo-1434030216411-0b793f4b4173?auto=format&fit=crop&w=800&q=80" 
                         alt="<%= test.title %>" class="listing-image">
                    <div class="listing-info">
                        <div class="flex justify-between items-start">
                            <h3 class="listing-title"><%= test.title %></h3>
                            <div class="text-sm"><i class="fa-solid fa-star"></i> 4.9</div>
                        </div>
                        <p class="listing-subtitle"><%= test.category %> • <%= test.duration %> mins</p>
                        <p class="listing-subtitle text-xs mt-1 line-clamp-1"><%= test.description || 'Professional test series for career growth.' %></p>
                        <div class="listing-price">
                            <% if (test.type === 'PAID') { %>
                                ₹<%= test.price %> <span>/ test</span>
                            <% } else { %>
                                Free <span>/ series</span>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% }) %>

            <% if (tests.length === 0) { %>
                <div class="col-span-full py-20 text-center">
                    <h2 class="text-2xl font-bold text-slate-400">No listings available right now</h2>
                    <p class="text-slate-500 mt-2">Check back later for new test series.</p>
                </div>
            <% } %>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>

</html>
